## 1. Состояние и создание альбома

- [x] 1.1 Заменить постоянно отображаемую форму создания альбома в sidebar на кнопку, Mantine Modal и валидацию обязательных полей с сохранением безопасных ошибок внутри модалки.
- [x] 1.2 После успешного `createAlbum` обновить список, выбрать id созданного черновика, закрыть модалку и подготовить его как текущую цель загрузки; при ошибке не закрывать модалку.

## 2. Контекстная загрузка и автоматическое добавление

- [x] 2.1 Условно скрыть весь workspace мультизагрузки для опубликованного альбома и вернуть его при выборе черновика, не отменяя существующие queue items.
- [x] 2.2 Проверить client sync после обработки: загрузка из выбранного черновика передаёт его `targetAlbumId`, а готовое медиа появляется в составе без ручного нажатия «Добавить» и без потери dirty-state.

## 3. Визуальное курирование медиа

- [x] 3.1 Заменить строковой список «Добавить готовые медиа» на адаптивную сетку карточек с защищённым JPEG-превью, именем, состоянием, безопасным fallback и действием добавления без дублей.
- [x] 3.2 Довести drag-and-drop перестановку карточек состава до явного и предсказуемого UX: валидные drop targets, dirty-state, блокировка редактирования опубликованного альбома и сохранение существующих кнопок перемещения.
- [x] 3.3 Проверить, что сохранение передаёт текущий визуальный порядок, сервер сохраняет уникальные позиции, а повторная загрузка альбома восстанавливает порядок и featured-признаки.

## 4. Проверка

- [ ] 4.1 Выполнить focused smoke flow: открыть модалку, создать черновик, загрузить synthetic fixtures, дождаться автоматического появления готовых фото, добавить готовое медиа по превью и изменить порядок drag-and-drop.
- [x] 4.2 Проверить, что у опубликованного альбома upload workspace отсутствует, а после выбора черновика снова отображается; отдельно проверить, что опубликованные действия состава остаются недоступными.
- [x] 4.3 Запустить `npm run lint --workspace share-web`, `npm run typecheck --workspace share-web` и `npm run build --workspace share-web`; при изменении API выполнить релевантные проверки `share-api`.
