## MODIFIED Requirements

### Requirement: Администратор управляет альбомами и их составом
Система SHALL позволять аутентифицированному администратору создавать, редактировать и удалять неопубликованные альбомы; назначать готовые медиа альбому; определять порядок без дублирующихся позиций; выделять избранные элементы; публиковать и снимать с публикации альбом. Форма создания альбома MUST быть скрыта до явного действия администратора и открываться в модальном окне. Для готовых медиа, доступных для назначения, интерфейс SHALL показывать превью, имя, состояние и действие добавления. Интерфейс SHALL поддерживать drag-and-drop перестановку состава альбома, а также сохранять доступные кнопки перемещения для клавиатуры и устройств без drag-and-drop. Интерфейс SHALL явно показывать несохранённые изменения и результат операции.

#### Scenario: Администратор открывает создание альбома
- **WHEN** администратор открывает админку
- **THEN** форма создания альбома не занимает постоянное место в рабочей области, а после нажатия кнопки создания открывается в модальном окне

#### Scenario: Администратор создаёт альбом через модальное окно
- **WHEN** администратор заполняет обязательные поля и подтверждает форму в модальном окне
- **THEN** создаётся неопубликованный альбом, модальное окно закрывается, а новый альбом становится выбранным для дальнейшей загрузки

#### Scenario: Администратор добавляет готовое медиа из списка
- **WHEN** в выбранном неопубликованном альбоме есть готовое медиа, ещё не входящее в его состав
- **THEN** интерфейс показывает карточку с доступным превью и именем медиа, а действие «Добавить» добавляет это медиа в локальный состав без дублирования

#### Scenario: Администратор публикует подготовленный альбом
- **WHEN** администратор публикует альбом хотя бы с одним готовым медиа
- **THEN** альбом становится доступен для публичных запросов главной и страницы альбома

#### Scenario: Администратор снимает альбом с публикации
- **WHEN** администратор снимает альбом с публикации
- **THEN** альбом и все его медиа перестают быть доступны через публичные маршруты галереи и медиа

#### Scenario: Администратор сортирует состав перетаскиванием
- **WHEN** администратор перетаскивает карточку медиа на новую позицию в неопубликованном альбоме и сохраняет состав
- **THEN** система сохраняет каждый элемент ровно с одной уникальной позицией, а повторное открытие альбома показывает новый порядок

#### Scenario: Администратор сохраняет порядок и избранные медиа
- **WHEN** администратор меняет порядок или признак избранного у медиа в неопубликованном альбоме и сохраняет изменения
- **THEN** система сохраняет каждый элемент ровно с одной уникальной позицией, а повторное открытие альбома показывает сохранённый порядок и признаки избранного

#### Scenario: Администратор сохраняет недопустимый состав альбома
- **WHEN** операция сохранения содержит медиа, которое ещё не готово, или невалидный состав
- **THEN** система отклоняет операцию, не меняет ранее сохранённый состав и показывает администратору конкретную причину отказа

### Requirement: Администратор загружает несколько медиа в выбранный альбом
Админка SHALL позволять выбрать несколько допустимых изображений за одну операцию и указать неопубликованный альбом назначения. Каждое медиа SHALL обрабатываться независимо. После успешной обработки система SHALL автоматически добавить медиа в выбранный альбом в порядке завершения загрузки, не требуя отдельного ручного сохранения состава. Медиа с ошибкой обработки SHALL не добавляться в альбом.

#### Scenario: Пакет фотографий успешно обработан
- **WHEN** администратор загружает несколько фотографий в выбранный неопубликованный альбом, и фотографии успешно обработаны
- **THEN** каждая готовая фотография автоматически появляется в составе выбранного альбома с уникальной позицией, а очередь показывает итог «готово»

#### Scenario: В пакете есть ошибка обработки
- **WHEN** одна фотография из пакета не проходит обработку, а остальные проходят успешно
- **THEN** успешные фотографии автоматически добавляются в альбом, ошибочная остаётся приватной и показывает безопасную причину ошибки с возможностью повторной загрузки или удаления

#### Scenario: Целевой альбом больше недоступен для автоматического добавления
- **WHEN** выбранный альбом удалён или опубликован до завершения обработки фотографии
- **THEN** фотография может стать готовой, но не добавляется автоматически в недоступный для редактирования альбом, а администратор видит это состояние и может назначить медиа вручную после выбора допустимого альбома

## ADDED Requirements

### Requirement: Администратор видит только применимые действия альбома
Интерфейс SHALL скрывать рабочую область мультизагрузки, когда выбран опубликованный альбом. Для чернового альбома рабочая область мультизагрузки SHALL быть доступна и SHALL использовать выбранный черновик как целевой альбом по умолчанию.

#### Scenario: Открыт опубликованный альбом
- **WHEN** администратор выбирает опубликованный альбом
- **THEN** окно и элементы мультизагрузки не отображаются в рабочей области этого альбома

#### Scenario: После опубликованного выбран черновой альбом
- **WHEN** администратор выбирает неопубликованный альбом
- **THEN** рабочая область мультизагрузки снова отображается и новые файлы направляются в выбранный черновик

